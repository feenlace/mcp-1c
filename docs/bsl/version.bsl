// Обработчик GET /version
// Возвращает версию расширения.
//
// Ответ: JSON {"version": "0.4.3"}

Функция ВерсияGET(Запрос)
    Результат = Новый Структура;
    Результат.Вставить("version", "0.4.3");

    Возврат ОтветJSON(Результат);
КонецФункции
